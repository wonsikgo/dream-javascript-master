"use strict";

/* 
    2. 변수 이름 짓기

     - 저장된 값을 잘나타낼 수 있는 의미 있는 이름

     - 변수규칙
        - 라틴문자(0-9, a-z, A-Z), _
        - 대소문자를 구분함
        - 추천: CamelCase (likeThis)
        - 한국어x, 예약어x, 숫자로시작x, 특수문자x (_, $ 제외)

 */
let apple;
let redApple;

// 나쁜예제
let number = 20;

let audio1;
let audio2;

// 좋은예제 (의미를 확실히 하는것이 좋음)
let myAge = 20;

let backgroundAudio;
let windAudio;

// 더 좋은 방법
let audioBackground;
let audioWind;
